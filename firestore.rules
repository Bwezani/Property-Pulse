rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Development-friendly rules: allow all authenticated users to read and write to all collections.
    // This resolves permission issues during initial setup and testing.
    
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
